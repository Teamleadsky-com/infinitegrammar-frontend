# Neon Database Connection
# Get this from: Netlify Dashboard > Site > Integrations > Neon > View database details
DATABASE_URL=postgresql://username:password@host.neon.tech/dbname?sslmode=require

# JWT Secret for Authentication
# Generate a random string (e.g., use: openssl rand -base64 32)
JWT_SECRET=your-secret-key-here

# Google Analytics Configuration
# Get these from: Google Analytics > Admin > Data Streams > Choose your stream > Measurement Protocol API secrets
VITE_GA_MEASUREMENT_ID=G-569909YP8G
VITE_GA_API_SECRET=your-ga-api-secret-here

# Optional: Environment indicator
NODE_ENV=development
